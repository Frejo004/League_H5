# 🔍 AUDIT GLOBAL — LEAGUE H5 (12/09/2026)

> Audit complet du projet : sécurité, architecture/qualité de code, performance, UI/UX, dépendances.
> Fait suite à `AUDIT_CORRECTIFS_TRACKER.md` (89% des 18 chantiers précédents terminés). Ce document couvre le reste du projet en profondeur, y compris ce que le tracker précédent indiquait comme "Terminé" à tort.

**Statut des corrections (mis à jour le 12/09/2026) :**
- ✅ [SEC-A] `claim_player_invite` — corrigé (migration `202609120001`) + lien d'invitation client corrigé (`InviteButton.tsx` utilisait déjà le slug devinable au lieu du vrai token — faille encore plus grave que documentée initialement, voir détail plus bas).
- ✅ [SEC-B] `get-turn-credentials` — fonction supprimée (non authentifiée et jamais utilisée par l'app). **Action manuelle restante : la supprimer aussi côté Supabase distant** (dashboard ou `supabase functions delete get-turn-credentials`), la suppression du code source ne l'undeploy pas.
- ✅ [SEC-C] RLS `transfers` — corrigée (migration `202609120002`), scopée par équipe au lieu d'admin-only.
- ✅ [SEC-D] RLS `match_lineups` — SELECT/UPDATE/DELETE restaurées (migration `202609120002`) ; corrige un bug visible : les compositions ne s'affichaient plus nulle part.
- ✅ [CODE-A] Widgets Dashboard fantômes — rebranchés (meilleur buteur, leader, top pronostiqueurs, suspensions actives) sur les 3 vues (Admin/Capitaine/Joueur) ; `KpiCard` et `ChessTournamentBanner` (code mort sans données réelles) supprimés.
- ✅ [MIN-A] `ScoreboardPage.tsx` — `useState(Date.now())` → `useState(() => Date.now())`.
- ✅ Dépendances — `npm audit fix` appliqué, 0 vulnérabilité restante.
- ✅ [UI-A]/[UI-B] Contrastes mode clair — corrigés sur ChatPage.tsx, GenericChat.tsx, CaptainPage.tsx (45 occurrences), PlayersPage.tsx, PlayerProfilePage.tsx (dont un graphique SVG avec couleurs `rgba(255,255,255,…)` fixes), MyStatsPage.tsx (dont un dégradé de texte qui devenait invisible en mode clair), et `StatusBadges.tsx` (contraste des badges de rôle/statut réécrit avec variante claire dédiée).
- ✅ [UI-C] Lien de navigation "Tournois" ajouté dans `Header.tsx` pour les 4 rôles (desktop + mobile + titre de page).
- ✅ [UI-E] Zones tactiles mobiles (cloche, chat, menu hamburger) portées à 44×44px dans `Header.tsx`.
- ✅ [UI-D] 2 `aria-label` ajoutés sur les boutons d'édition icône-seule de `CaptainPage.tsx`.
- ✅ [CODE-B] Fuite de canal Realtime dans `CaptainPage.tsx` (`broadcastUpdate`) — corrigée, un seul canal souscrit et réutilisé au lieu d'un nouveau par appel jamais nettoyé.
- ✅ [CODE-C] Doublon de canal Realtime sur `match_events` — le canal dédié de `useMatchEvents` (useMatchLive.ts) a été supprimé ; `useRealtimeMatch` (toujours monté en parallèle sur les mêmes pages) invalide déjà la même query.
- ✅ [CODE-E] Code-splitting du bundle — `vite.config.ts` sépare désormais react/react-dom, react-router, @supabase/supabase-js et framer-motion en chunks vendor dédiés (`build.rolldownOptions.output.manualChunks`, ce projet utilise Vite 8 + Rolldown). Chunk principal : 607 kB → 130 kB.
- ✅ [CODE-H] Service worker — `AdminPage-*.js` et `CaptainPage-*.js` exclus du precache à l'installation (`injectManifest.globIgnores`), mis en cache à la demande seulement au premier accès.
- ✅ [CODE-G] Mémoïsation de `MatchDetailPage.tsx` (assistMap/sortedGoals/voteMap/topMvpIds/mvpPlayers/totalVotes/maxVotes/liveScore) — corrigé en remontant ces calculs avant les early returns (loading/match introuvable) avec des accès `match?.champ` optionnels, suivant le pattern déjà en place pour `displayGoals`/`matchStats`/`allMatchPlayers`. Ces valeurs ne sont plus recalculées à chaque tick de l'horloge live.
- ✅ [CODE-F] Sur-fetching partiel corrigé : `useMatch`/`useMatchBySlug` (useMatches.ts) pollaient toutes les 5s en live en plus de l'invalidation Realtime déjà faite par `useRealtimeMatch` — intervalle porté à 20s (filet de sécurité si Realtime rate un événement, plus l'invalidation immédiate normale). `useLiveReactions` (5s) n'a PAS été touché : son polling sert à faire glisser une fenêtre de 30s (les réactions expirent avec le temps même sans nouvel INSERT), ce n'est pas un doublon — Realtime et polling y ont des rôles différents.
- ℹ️ [SEC-E] Vérifié dans l'historique des migrations : `202606230006_drop_chat_participants_policies.sql` supprime bien (avec `IF EXISTS`, idempotent) la policy `chat_conv_insert` incriminée et les autres policies legacy du chat. Aucun code à corriger ici — juste à confirmer que cette migration a bien été appliquée en production (déploiement standard, pas un bug de code).
- ✅ [SEC-F] `get-metered-config` bloquait tout appelant non-admin/non-reporter, y compris les spectateurs qui voulaient juste regarder un live déjà démarré. Ajout d'un paramètre `role` : la vérification stricte et la création de room restent réservées au broadcaster, un viewer authentifié peut désormais rejoindre un live existant. **Nécessite un redéploiement de la fonction Edge côté Supabase** (`supabase functions deploy get-metered-config`) pour prendre effet en production.
- ✅ Nettoyage code mort : tous les `no-unused-vars` ESLint résolus (imports/variables orphelins dans ProtectedRoute, AdminLiveControls, useNews, MatchesPage, ProfilePage, StandingsPage, AdminTransfersPage).
- ✅ [MIN-C] Les 107 `no-explicit-any` restants ont été éliminés (26 fichiers, typage strict via `src/types/database.ts`). ESLint : 137 → 1 erreur pré-existante non liée (laissée intacte volontairement, voir commit `0f441a0`).
- ✅ [CODE-I] Images hero converties en WebP (`leagueH5-bg_login.jpg` 87→49 kB, `leagueH5-bg_bg.jpg` 218→196 kB) + `fetchPriority="high"` sur l'image landing (au-dessus de la ligne de flottaison).
- ✅ Audit de contraste étendu au reste des 61 fichiers `text-white` : `RulesPage.tsx` (14 corrigées), `PlayoffsPage.tsx` (8), `PlayerRow.tsx` (4), `AdminSchedulePage.tsx` (4), `MyTeamPage.tsx` (1) — soit 31 corrections supplémentaires. Le reste (>200 occurrences) confirmé correct après vérification contextuelle : fonds fixes (couleurs d'équipe, badges pleins, podiums, overlays vidéo/live) qui ne doivent pas changer avec le thème.
- ⚠️ Nouveau constat (hors périmètre initial, non corrigé) : `PlayerStatsDrawer.tsx` (composant capitaine) utilise `style={{backgroundColor: '#161B22'}}` codé en dur au lieu de la variable de thème — même défaut que `ChatPanel.tsx` déjà identifié : le fond ne s'adapte jamais au mode clair (mais reste cohérent avec lui-même, donc pas d'inversion de contraste comme sur les autres pages — juste un panneau qui reste sombre alors qu'il pourrait suivre le thème).
- ✅ [CODE-D] Mutualisation partielle des canaux Realtime globaux (actifs sur toute page authentifiée) : `ChatToastProvider` (3→1 canal), `useNotifications` (5→2 canaux, le canal admin restant séparé car sa condition d'activation diffère). Total sur une page de match : ~10-11 → ~7 canaux simultanés. `useChatUnread`/`usePresence` déjà à 1 canal chacun (rien à fusionner en interne) ; les fusionner entre eux ou avec `useNotifications` demanderait de restructurer plusieurs hooks/contextes à la fois — laissé de côté, trop de surface de risque pour un gain marginal sans possibilité de test visuel.
- ✅ [CODE-J] `AdminSchedulePage.tsx` : les 7 appels Supabase directs de nettoyage cascade (annulation de match) extraits dans `useCancelMatchCleanup()` (`useMatches.ts`), verbatim, cohérent avec le reste des pages admin.
- ✅ [CODE-K] Découpage des 3 plus gros fichiers en composants déjà autonomes (relocalisation pure, aucune logique modifiée) :
  - `CaptainPage.tsx` : 1543 → 261 lignes (8 composants + helpers → `src/components/captain/`)
  - `AdminSchedulePage.tsx` : 1090 → 310 lignes (algorithme de calendrier → `src/lib/matchScheduling.ts`, `MatchDateEditor` → `src/components/admin/`)
  - `MatchDetailPage.tsx` : 1443 → 1302 lignes (`MatchStatsView`, `GoalEvent` → `src/components/matches/`)
- ⏳ Reste à faire (mineur) : le fond figé de `ChatPanel.tsx`/`PlayerStatsDrawer.tsx` si on veut les rendre thème-adaptatifs.

**Audit terminé.** Tous les chantiers identifiés (sécurité critique, RLS, performance, contrastes, architecture) sont traités ou explicitement documentés comme laissés de côté avec justification.

**Méthode** : `npm run lint`, `npm run build:check`, `npm audit`, lecture manuelle des migrations SQL (97 fichiers), des Edge Functions, et revue ciblée par 4 sous-agents (sécurité, architecture, performance, UI/UX) sur les fichiers réels du code.

---

## 🔴 CRITIQUE — à corriger en priorité absolue

### [SEC-A] `claim_player_invite` — élévation de privilège sans authentification
- **Fichiers** : `supabase/migrations/20240011_player_invites.sql:107`, `20260517150500_support_slug_invites.sql:34`, `202609040001_phase1_critical_fixes.sql:11`
- **Risque** : la fonction `SECURITY DEFINER` est `GRANT`ée à `anon`/`authenticated` mais ne vérifie **jamais** que `p_user_id = auth.uid()`. Un attaquant non authentifié peut appeler ce RPC avec un slug de joueur valide (prévisible : `prénom-nom`) et un `p_user_id` arbitraire pour lier/écraser le compte d'une victime, ou s'approprier une invitation destinée à quelqu'un d'autre.
- **Fix** : ajouter `IF p_user_id <> auth.uid() THEN RAISE EXCEPTION ...` dans la fonction, ou supprimer le paramètre `p_user_id` et utiliser `auth.uid()` directement côté SQL.
- **Historique** : cette faille existe depuis l'origine ; le correctif d'urgence de septembre a rétabli les GRANT sans jamais ajouter le contrôle d'identité manquant.

### [SEC-B] Edge Function `get-turn-credentials` sans aucune authentification
- **Fichier** : `supabase/functions/get-turn-credentials/index.ts`
- **Risque** : contrairement à `get-metered-config` (qui vérifie bien rôle admin / reporter vidéo), cette fonction ne vérifie ni token ni rôle. N'importe qui connaissant l'URL du projet Supabase (exposée côté client) peut l'appeler en boucle et consommer le quota payant de l'API Metered (TURN), sans être connecté — coût direct et risque de déni de service économique.
- **Fix** : exiger un `Authorization` header valide et vérifier `auth.getUser()` avant d'appeler l'API Metered (même pattern que `get-metered-config`).

---

## 🟠 MAJEUR

### Sécurité
- **[SEC-C] RLS `transfers` incohérente avec le code applicatif** — `202606230004_fix_rls_performance.sql:658-667` restreint l'UPDATE/INSERT aux seuls `admin`, alors que `src/hooks/useTransfers.ts:102-232` (`approveAsHomeCaptain`, `approveAsAwayCaptain`) fait des `.update()` directs en tant que capitaine → ces appels échoueront silencieusement sous RLS. Il faut soit re-scoper la policy par équipe (capitaine ne peut modifier que les transferts de sa propre équipe), soit refondre le workflow d'approbation.
- **[SEC-D] `match_lineups` n'a plus de policy SELECT/UPDATE/DELETE**, seulement INSERT (`202606230004_fix_rls_performance.sql:1268-1290`). `update_match_lineup()` fait un DELETE avant INSERT : bloqué par RLS → doublons de composition probables (fail-closed, pas de risque sécu mais régression fonctionnelle).
- **[SEC-E] Policy legacy `chat_conv_insert` (`USING(true)`)** sur `chat_conversations` documentée comme "à corriger manuellement dans le SQL Editor" (`202606230001...sql:477`) — aucune garantie que ce correctif manuel a été appliqué en prod, ni que les tables legacy (`chat_conversations/participants/messages`) ont bien été supprimées comme prévu par `202606230006_drop_chat_participants_policies.sql`. À vérifier directement en base de production.
- **[SEC-F]** `get-metered-config` bloque aussi les spectateurs (ni admin ni reporter vidéo) qui voudraient simplement regarder un live — probable régression produit à confirmer (hors périmètre sécurité pur).

### Qualité de code / Architecture
- **[CODE-A] Widgets Dashboard fantômes — régression de fonctionnalité** : `DashboardPage.tsx` définit `TopScorerCard` (258), `LeaderCard` (292), `MiniLeaderboard` (618), `ActiveSuspensionsWidget` (753) — tous alimentés par des hooks (`useScorers`, `useStandings`, `useLeaderboard`, `useSuspensions`) et les données calculées/passées en props — mais **jamais rendus** dans `AdminDashboardContent`/`CaptainDashboardContent`/`PlayerDashboardContent`. Le dashboard n'affiche donc plus le meilleur buteur, le leader du classement, le top pronostiqueurs, ni les suspensions actives, alors que toute la plomberie existe. À rebrancher (ou supprimer proprement si abandonné).
- **[CODE-B] Fuite de canal Realtime** : `CaptainPage.tsx:582`, `broadcastUpdate` crée un `supabase.channel(...)` et l'utilise pour `.send()` à **chaque appel** (changement de formation/titulaire) sans jamais `removeChannel` — accumulation de connexions sur une session longue.
- **[CODE-C] Doublon de canal Realtime** : `useRealtimeMatch` (`useRealtime.ts:72`) et `useMatchEvents` (`useMatchLive.ts:52`) écoutent tous les deux la table `match_events` via deux canaux distincts sur `MatchDetailPage` — à fusionner.
- **[CODE-D] ~10-11 canaux Websocket simultanés** ouverts par utilisateur sur une simple page de match (chat toasts, présence, notifications, match, événements...) — charge serveur multipliée inutilement, à mutualiser.
- **[CODE-E]** Pas de `manualChunks` dans `vite.config.ts` → chunk vendor+app unique de 607 kB, invalidé à chaque déploiement (aucun bénéfice de cache long terme pour React/Supabase/Framer Motion).
- **[CODE-F]** Sur-fetching : `useMatches.ts` (staleTime:0 + refetchInterval 5s) et `useMatchLive.ts` `useLiveReactions` (staleTime:0 + refetchInterval 5s) **cumulés** avec une invalidation Realtime sur les mêmes données — double mécanisme redondant en live.
- **[CODE-G]** Calculs non mémoïsés dans `MatchDetailPage.tsx:521-564` (`sortedGoals`, `voteMap`, `mvpPlayers`, `liveScore`...) recalculés à chaque render, potentiellement plusieurs fois/seconde en live, alors que d'autres calculs du même fichier sont bien en `useMemo`.
- **[CODE-H] Service Worker précache quasi toute l'app** (109 entrées / 1970 KiB), y compris les chunks admin (124 kB) et capitaine (54 kB) qu'un simple spectateur ne visitera jamais — annule une partie du bénéfice du lazy-loading par route.
- **[CODE-I] Images non optimisées** : `leagueH5-bg_login.jpg` (87 kB) et `leagueH5-bg_bg.jpg` (218 kB) en JPEG brut, sans WebP/AVIF, sans `srcset`, sans `loading="lazy"` sur la landing — chargées en pleine résolution desktop même sur mobile, impact direct sur le LCP de la page de login (première page vue par tout utilisateur).
- **[CODE-J]** `AdminSchedulePage.tsx` fait 9 appels `supabase.from(...)` directs alors que toutes les autres pages admin délèguent à des hooks dédiés — incohérence d'architecture.
- **[CODE-K]** Fichiers-pages surdimensionnés à découper : `CaptainPage.tsx` (1529 lignes), `MatchDetailPage.tsx` (1426), `DashboardPage.tsx` (1363), `AdminSchedulePage.tsx` (1089), `PublicMatchDetailPage.tsx` (921).

### UI/UX
- **[UI-A] Le correctif UI-01 du tracker précédent est très incomplet.** Le tracker affirmait ChatPage "OK a priori" — c'est faux : `--color-chat-panel` vaut blanc en thème clair, donc tout `text-white` posé dessus (titres, noms de contacts, placeholders — `ChatPage.tsx:172,209,353,375,721,787`) devient illisible en mode jour. Le vrai périmètre non corrigé est bien plus large que prévu : **CaptainPage.tsx (quasi aucun `dark:` sur ~1500 lignes), DashboardPage.tsx, PlayersPage.tsx, PlayerProfilePage.tsx, MyStatsPage.tsx, GenericChat.tsx** — ces pages semblent n'avoir jamais été testées en mode clair.
- **[UI-B] `StatusBadges.tsx`** (`RoleBadge`, `MatchStatusBadge`, `TransferStatusBadge`, `SpectatorStatusBadge`) utilise systématiquement `bg-{couleur}-500/15 text-{couleur}-400`, un contraste conçu pour fond sombre : sur fond clair le texte devient à peine lisible. Impact large car ces badges sont utilisés partout dans l'app.
- **[UI-C] PROD-02 incomplet malgré le journal** : la clarification visuelle du module Tournois d'échecs (TournamentsPage, LandingPage) est bien faite, **mais aucun rôle n'a `/tournaments` dans `NAV_BY_ROLE` (Header.tsx:39-96)** — la page n'est atteignable que par URL directe, sans lien de menu nulle part (desktop, mobile drawer, bottom nav). Fonctionnalité orpheline.
- **[UI-D]** `CaptainPage.tsx` : quasiment aucun `aria-label` sur ~1500 lignes (ex. bouton crayon d'édition du logo d'équipe, `:1430-1438`, sans label).
- **[UI-E]** `Header.tsx` : icônes cloche/chat/menu avec `p-1.5` sur icônes 17-19px → zone tactile ≈ 29-32px, sous le seuil de 44px recommandé, notamment sur le bouton hamburger utilisé à chaque navigation mobile.

---

## 🟡 MOYEN / MINEUR

- **[MIN-A]** `ScoreboardPage.tsx:18` — `useState(Date.now())` (impur, exécuté à chaque render) au lieu de `useState(() => Date.now())` comme fait correctement ailleurs (`MatchDetailPage.tsx:223`, `MatchLineups.tsx:98`). Un vrai bug de rendu React, détecté par le linter React Compiler.
- **[MIN-B]** Autres appels `new Date()`/`Date.now()` impurs mais sans état associé (impact réel faible, dérive possible à minuit) : `CaptainPage.tsx:60`, `ChatPage.tsx:28`, `MatchesPage.tsx:20`, `PublicMatchesPage.tsx:180`, `MatchFeedbackPage.tsx:21`, `DashboardPage.tsx:35`, `LandingPage.tsx:222`.
- **[MIN-C]** ~40 `no-explicit-any` restants (voir liste ESLint ci-dessous), concentrés surtout dans `usePlayerProfile.ts` (21 occurrences) — facilement corrigibles car `src/types/database.ts` (1779 lignes, écrit à la main) contient déjà les types complets à réutiliser.
- **[MIN-D]** Code mort confirmé par ESLint : imports/vars inutilisés dans `DashboardPage.tsx`, `ProfilePage.tsx`, `MatchesPage.tsx`, `StandingsPage.tsx`, `AdminTransfersPage.tsx`.
- **[MIN-E]** `ScorersPage.tsx` réimplémente un état vide en dur (`text-slate-400/300/500`) au lieu du composant `<EmptyState>` partagé utilisé partout ailleurs.
- **[MIN-F]** `AdminSchedulePage.tsx:818` — `matchdays` recalculé à chaque render sans mémoïsation (volume modéré, impact faible).

---

## 📦 Dépendances

- `npm audit` : **3 vulnérabilités "high"** — `react-router`/`react-router-dom` (DoS, CSRF, open redirect, XSS) et `ws` (fuite mémoire, DoS) → corrigibles via `npm audit fix` (mise à jour vers react-router-dom 7.18.3).
- Plusieurs dépendances mineures en retard (`@supabase/supabase-js` 2.105→2.116, `lucide-react` 1.14→1.45, `tailwindcss` 4.2.4→4.3.3, etc.) — pas critique mais à planifier.
- `framer-motion` 12.38 → une version majeure 13.x est disponible (à tester avant upgrade, breaking changes possibles).

---

## ✅ Points positifs confirmés (ne pas re-toucher)

- Aucun secret (clé `service_role`, clé API privée) trouvé dans le code source client ; `.env`/`.pem` correctement gitignorés.
- Aucune faille XSS trouvée (pas de `dangerouslySetInnerHTML` avec données utilisateur).
- `get-metered-config` valide correctement l'autorisation serveur (admin ou reporter vidéo désigné) avant de délivrer l'accès WebRTC.
- La majorité des tables sensibles (`profiles`, `teams`, `players`, `matches`, `mvp_votes`, `spectators`, `suspensions`, `bet_slips`, `team_messages`, `channel_messages`, `dm_messages`) ont des RLS cohérentes et bien conçues.
- Le lazy-loading par route (`React.lazy` dans `router.tsx`) est bien en place pour les 32 routes.
- `AdminLiveControls.tsx` respecte déjà le seuil tactile de 44px (correctif précédent validé).
- `npm run build:check` (tsc + vite build) passe sans erreur.

---

## 📋 Détail brut ESLint (137 erreurs, 2 warnings au 12/09/2026)

Voir sortie complète de `npm run lint`. Répartition : ~40 `no-explicit-any`, ~15 `no-unused-vars`, 1 `prefer-const`, 1 `react-hooks/purity` (ScoreboardPage), reste réparti sur `AdminSchedulePage.tsx`, `AdminSeasonsPage.tsx`, `PlayoffsPage.tsx`, `RulesPage.tsx`.

---

## Priorité recommandée

1. **[SEC-A]** `claim_player_invite` — critique, exploitable sans authentification.
2. **[SEC-B]** Ajouter l'authentification sur `get-turn-credentials`.
3. **[SEC-C]/[SEC-D]** Clarifier les RLS `transfers` et `match_lineups` avant qu'un capitaine ne découvre que ses actions échouent silencieusement.
4. **[CODE-A]** Décider du sort des widgets Dashboard fantômes (rebrancher ou supprimer).
5. **[UI-A]/[UI-B]** Reprendre l'audit de contraste mode clair sur tout le périmètre réel (pas seulement les pages déjà "corrigées").
6. `npm audit fix` pour les 3 vulnérabilités high.
7. Le reste (perf, mémoïsation, code-splitting, ESLint) en continu.

---

## Mise à jour UI — Checklist anti-look généré (26/09/2026)

Périmètre : tokens globaux, landing publique, profils joueur, composants partagés de cartes/hero, navigation, formulaires et module tournois. Les constats sécurité, base Supabase et architecture ci-dessus restent ceux de l'audit précédent et n'ont pas été réexécutés dans ce chantier visuel.

### Visuel

- ✅ **01 — Palette violet/bleu** : primaire global converti en vert gazon, accent secondaire or trophée. Fonds par page réalignés; l'accent violet du module tournois d'échecs a été remplacé par l'or. Les couleurs d'équipe et les couleurs fonctionnelles (erreur, danger, badges informatifs) restent propres à leur sens métier.
- ✅ **02 — Titres en dégradé** : noms/rating du profil et du tableau joueur utilisent désormais des couleurs pleines. Le titre principal de la landing utilise le vert de marque plein au lieu d'un texte transparent détouré.
- ✅ **03 — Emojis dans les titres** : aucun emoji décoratif trouvé dans les `h1`/`h2`. Les symboles emoji repérés décrivent des réactions ou types d'événements de match, pas des titres.
- ✅ **04 — Inter partout** : import et font-sans Inter supprimés; Barlow sert le texte courant, Barlow Condensed la hiérarchie sportive et les scores.
- ✅ **05 — Bordures colorées** : cartes système et featured utilisent une bordure neutre. Les marqueurs de couleur d'équipe/statut restent intentionnels.
- ✅ **06 — Glassmorphism généralisé** : `glass-morphism`, `glass-card` et `glass-stat` ont des fonds opaques et ombres discrètes, sans flou. Les flous conservés sont des overlays sur vidéo/image, menus et modales superposés, ainsi que la barre sticky de diffusion, où ils séparent réellement le contenu du média.
- ✅ **07 — Contraste dark/light** : fonds et niveaux de texte redéfinis pour les deux thèmes; contraste WCAG calculé pour le texte primaire/secondaire/muet : dark `17,39:1`, `7,10:1`, `6,07:1`; light `15,52:1`, `9,30:1`, `5,84:1`. Les six combinaisons passent AA pour le texte normal. Cela valide les tokens, pas toutes les associations de couleur métier codées dans les écrans.
- ✅ **08 — Trois icon boxes alignées** : grille de fonctions de la landing remplacée par une liste de huit outils en deux colonnes, avec icône simple, séparateurs et descriptions concrètes.
- ✅ **09 — Badge au-dessus du titre** : aucun badge promotionnel « New/Powered by » trouvé. Le nom de saison affiché sur la landing est une information de contexte réelle.
- ◐ **10 — Icônes Lucide partout** : Lucide reste le jeu d'icônes uniforme du projet. Pas de retrait massif : les icônes identifient les actions, événements, catégories et destinations. À revoir au cas par cas si de nouveaux usages purement décoratifs sont ajoutés.
- ✅ **11 — Composants génériques** : aucun shadcn n'est installé. Les composants CSS partagés sont personnalisés via les tokens de marque; boutons, champs, cartes, focus et états actifs utilisent le vert/or plutôt que le primaire bleu historique.
- ✅ **12 — Fade-in au scroll** : aucune logique IntersectionObserver/reveal au scroll trouvée. Les transitions de route et d'état local restent présentes; elles ne sont pas déclenchées section par section au scroll.
- ✅ **13 — Traînée de curseur** : aucun effet de curseur trouvé.
- ✅ **14 — Hover par opacité** : le bouton texte de ligne joueur utilise désormais une variation de couleur. Les opacités restantes révèlent des commandes contextuelles ou indiquent un état désactivé.
- ◐ **15 — Espacement** : échelle documentée sur une base de 4 px, avec rythme principal de 8 px (`4/8/12/16/24/32/48`). Les pages possèdent encore des valeurs Tailwind ponctuelles; normaliser toutes les classes serait une migration séparée et risquée.
- ✅ **19 — Space Grotesk + Instrument Serif** : aucune de ces deux polices n'est utilisée; paire Barlow/Barlow Condensed cohérente avec les codes de diffusion sportive.
- ✅ **20 — Grain sur dégradé** : aucune texture de bruit/grain trouvée. La grille et les tracés de terrain présents sont des motifs sportifs identifiables, sans grain plaqué.

### Texte et ton

- ◐ **16 — Em dash** : pas d'usage répétitif repéré dans les textes de la landing; les tirets visibles ailleurs servent principalement de valeur absente, plage ou relation de score. Ponctuation des contenus éditoriaux existants non normalisée en masse.
- ✅ **17 — Buzzwords** : la promesse de la landing est remplacée par les fonctions présentes dans l'application (live, calendrier, classement, statistiques, messagerie, discipline et vote). Les descriptions génériques « infrastructure digitale », « ultra-réactive », « heatmaps » et « instant replay » ont été retirées.
- ✅ **18 — Italique serif d'emphase** : aucune police serif d'accent n'est utilisée. L'italique systématique de plusieurs titres/textes d'accroche de la landing a été retiré; l'italique restant dans les écrans sportifs est ponctuel et non serif.

### Changements et validation

- Fichiers de production modifiés : `src/index.css`, `src/components/layout/AppLayout.tsx`, `src/components/auth/AuthLayout.css`, `src/components/captain/PlayerRow.tsx`, `src/components/ui/PageHero.tsx`, `src/pages/LandingPage.tsx`, `src/pages/MyStatsPage.tsx`, `src/pages/ProfilePage.tsx`, `src/pages/TournamentsPage.tsx`, `src/pages/TournamentDetailPage.tsx`.
- `eslint.config.js` fixe explicitement la racine TypeScript et ignore `.kilo/**`, worktree imbriqué qui faisait échouer l'analyse du dépôt.
- `npm run build:check` : réussi (TypeScript + Vite).
- `npm test -- --run` : réussi, 8 tests sur 8 dans 2 fichiers.
- `npm run lint` : 1 erreur React préexistante dans `AuthLayout.tsx` (setState synchrone dans un effet) et 2 warnings de dépendances de hooks (`ChatToastProvider.tsx`, `useNetworkStatus.ts`). Aucun diagnostic ne concerne les fichiers de design/copie modifiés.
- `npm install` a rétabli les dépendances locales manquantes; le lockfile a été restauré sans diff. L'installation a signalé des avertissements de moteur Node pour jsdom et sous-dépendances (Node local `24.9.0`, attendu `24.15.0` ou plus récent).
- À contrôler visuellement avant mise en production : landing, page profil joueur, pages d'échecs en thème clair et sombre, largeur mobile, ainsi que les overlays de vidéo. Le calcul WCAG ci-dessus ne remplace pas un audit de toutes les couleurs d'équipe personnalisées.
